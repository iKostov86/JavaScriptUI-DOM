//var container = document.getElementById('svg-container'),
//    svg = document.getElementById('the-svg');

//svg.setAttribute('width', (svg.getAttribute('clientWidth')).toString());
//svg.setAttribute('height', (svg.getAttribute('clientHeight')).toString());

//svg.setAttribute('width', (container.clientWidth).toString());
//svg.setAttribute('height', (container.clientHeight).toString());