/* globals $ */
$.fn.gallery = function (col) {
    var col = col || 4;
    var $root = $('#root');
    var $gallery = $('#gallery');
    var $galleryList = $('.gallery-list');
    var $selected = $('.selected');

    $galleryList.click(function (ev) {

    });
};